[package]
name = "lavan"
version = "0.0.4-unstable"
authors = ["Sabi <sabitheotome@gmail.com>"]
description = "Lavan is a parser library"
license = "GPL-3.0"
repository = "https://github.com/sabitheotome/lavan"
edition = "2021"

[dependencies]
macros = { path = "./macros" }
either = { version = "1.9.0", optional = true }
derive_more = "0.99.17"

[dev-dependencies]

[features]
default = ["either"]
either = ["dep:either"]
experimental = []

[profile.dev]
opt-level = 2

[workspace]
members = ["macros"]
